import 'package:flutter/material.dart';

class GroupMessaging extends StatelessWidget {
  const GroupMessaging({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('GROUP'));
  }
}
