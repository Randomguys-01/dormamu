import 'package:flutter/material.dart';

class DirectMessaging extends StatelessWidget {
  const DirectMessaging({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('DIRECT'));
  }
}
